import LetterRainClient from './LetterRainClient'

export const metadata = {
 title: 'Letter Rain — Count the Letters | MemGenius',
 description: 'Letters fall from the sky. Count how many times the target letter appears. Train your selective attention.',
}

export default function LetterRainPage() {
 return <LetterRainClient />
}
