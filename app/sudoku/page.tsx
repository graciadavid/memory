import SudokuClient from './SudokuClient'

export const metadata = {
  title: 'Sudoku — MemGenius',
  description: 'Solve Sudoku puzzles as fast as possible. World ranking.',
}

export default function SudokuPage() {
  return <SudokuClient />
}
